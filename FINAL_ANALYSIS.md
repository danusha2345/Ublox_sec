# 🎯 Итоговый анализ подписей U‑blox (обновлено: 2025‑11‑20)

**Источник лога:** `logs_combined.bin` (объединенный лог)
**Подписей извлечено:** 2768 (было 1876)
**Разметка payload (108 байт):** `Version(2) | PacketCount(2) | SHA256(32) | SessionID(24) | R(24) | S(24)`  
**Повторы nonce:** не обнаружены.

---

## 📊 Статистика утечки
- r_bits: min **178** (утечка 14 бит), max 192, avg 191.02
- Распределение (утечка = 192 − r_bits):
  - 14: 1; 10–9: 4; 8–6: 27; 5: 21; 4: 46; 3: 117; 2: 240; 1: 488; 0: 932

Интерпретация: «сильных» подписей (утечка ≥6 бит) мало (27 шт.), поэтому нужна BKZ с блоком 32+ и/или расширенный набор (200–250 подписей).

---

## 📂 Файлы
- `sigs_combined.csv` — **2768 подписей** (основной файл для атаки).
- `sigs_new.csv` — 1876 подписей (старый набор).
- `hnp_capture.csv` — payload в hex.
- `plots/` — графики.

---

## 🛠️ Скрипты атаки
| Скрипт | Вход | Замечания |
|--------|------|-----------|
| `correct_lattice_attack.py` | hnp_capture.csv | LLL на малом наборе (быстро, но слабая редукция) |
| `fast_lattice_attack_v2.py` | sigs_new.csv | Быстрая BKZ, параметры `--top`, `--bkz` |
| `bkz_heavy_attack.py` | sigs_new.csv | Длительная BKZ с прогрессом, расписание блоков |
| `sage_lattice_attack.sage` | sigs_new.csv / hnp_capture.csv | BKZ в SageMath (можно поднять block_size) |

Данные z рассчитываются корректно: `sha = payload[0x04:0x24]`, `session = payload[0x24:0x3C]`, `z = fold( SHA256(sha||session) )`.

---

## 🚀 Текущий статус атак
- Быстрые попытки BKZ (block 32–34, top 80–200) решения не дали.
- LLL/малые базы тоже без результата.
- Ключ пока **не найден**.

---

## 📌 Рекомендуемый запуск
Быстрая проверка:
```bash
. .venv/bin/activate
python fast_lattice_attack_v2.py --top 80 --bkz 32
```

Долгая, с прогрессом (часы):
```bash
python bkz_heavy_attack.py --top 200 --blocks 30,32,34,36,38 --loops 4
```

Если есть SageMath — попробовать увеличить block_size в `sage_lattice_attack.sage` до 30–35 на top 200–256 подписей.

---

## 🔭 Что может улучшить шансы
1. Собрать подписи с ещё меньшими r_bits (<178) — усилит утечку.  
2. Увеличить BKZ block до 36–38 и loops ≥4.  
3. Попробовать комбинировать только самые «тонкие» 60–120 подписей для ускорения глубокого BKZ.  
4. Проверить другие логи/дни на предмет сильного смещения RNG.

---

## 📝 Быстрый справочник по полям UBX‑SEC‑SIGN
```
Offset | Size | Поле
0x00   | 2    | Version (LE)
0x02   | 2    | Packet Count (LE)
0x04   | 32   | SHA256 field
0x24   | 24   | Session ID
0x3C   | 24   | R (BE)
0x54   | 24   | S (BE)
```

z = fold( SHA256( SHA256_field || SessionID ) ), XOR последних 8 байт в первые 8. 
